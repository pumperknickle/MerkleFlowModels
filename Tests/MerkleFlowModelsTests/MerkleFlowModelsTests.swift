import XCTest
@testable import MerkleFlowModels

final class MerkleFlowModelsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MerkleFlowModels().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
